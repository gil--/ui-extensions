import {ProgressBar as BaseProgressBar} from '@shopify/admin-ui-extensions';
import {createRemoteReactComponent} from '@remote-ui/react';

export const ProgressBar = createRemoteReactComponent(BaseProgressBar);
